#pragma once

#include <av/common.hpp>

namespace av
{

#if 0
class StreamBase
{
public:
	explicit StreamBase(AVStream* stream)
	    : stream_(stream)
	{}

private:
	AVStream* stream_;
};
#endif

}// namespace av
